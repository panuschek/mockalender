spring:
  application:
    name: mockalender
  datasource:
    url: jdbc:h2:mem:mockalender
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect